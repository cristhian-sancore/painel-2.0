version: '3.8'
services:
  datacenter-panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: datacenter-panel
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
